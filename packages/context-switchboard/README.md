# Context Switchboard

Experimental, installable scaffold for explicit local context profiles and routing receipts. The current tool `context_switchboard_status` reports scope only; it does not yet modify context. See the root roadmap before production use.
